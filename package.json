{
  "name": "nimiq-branding-cli",
  "version": "0.1.0",
  "description": "Scaffold pixel-accurate Nimiq-branded UI components — Vue 3 or plain HTML/CSS — from a verified registry.",
  "type": "module",
  "bin": {
    "nq": "./bin/nq.js"
  },
  "scripts": {
    "verify": "node scripts/verify.mjs",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "pixelmatch": "^5.3.0",
    "playwright": "^1.49.0",
    "pngjs": "^7.0.0"
  }
}
